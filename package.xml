<?xml version="1.0"?>
<package format="2">
  <name>vitulus_mapping</name>
  <version>0.1.0</version>
  <description>Mapping v3 — mapping with known poses (gated insertion -> octomap 3D
  evidence archive, trajectory DEM -> grid_map terrain, ground-relative obstacle
  band raster). Runs alongside the existing stack; localization is untouched.</description>
  <maintainer email="robert@lacina.dev">user</maintainer>
  <license>MIT</license>

  <buildtool_depend>catkin</buildtool_depend>

  <depend>octomap</depend>
  <depend>rospy</depend>
  <depend>std_msgs</depend>
  <depend>std_srvs</depend>
  <depend>sensor_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>grid_map_msgs</depend>
  <depend>tf2_ros</depend>
  <depend>ublox_msgs</depend>
  <depend>vitulus_msgs</depend>

  <exec_depend>octomap_server</exec_depend>
  <exec_depend>laser_geometry</exec_depend>

</package>
